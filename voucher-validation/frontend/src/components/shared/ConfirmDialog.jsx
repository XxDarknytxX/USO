// src/components/shared/ConfirmDialog.jsx
// Legacy import path — re-exports the design-system primitive so existing
// callers (VoucherDetailModal, etc.) keep working without a sweep.
// Prefer importing from "@/components/ui" in new code.

export { default } from "../ui/ConfirmDialog";
